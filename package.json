{
  "name": "remoteNation-desktop",
  "main": "index.html",
  "scripts": {
  	"start": "rm -f app.nw && zip -r app.nw . && forever start master.js && nodewebkit app.nw && forever stop master.js"
  },
  "dependencies": {
  	"jade": "*",
    "nw-flash-trust": "*"
  },
  "chromium-args": "--disable-web-security",
  "webkit": {
    "plugin": true
  },
  "window": {
    "frame": true,
    "toolbar": true
  }
}
